services:
  app:
    build:
      context: .
      dockerfile: .docker/app/Dockerfile.dev
    command: npm run dev
    environment:
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "3000:3000"